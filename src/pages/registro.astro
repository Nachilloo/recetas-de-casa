---
import Layout from "../layouts/Layout.astro";
import AuthForm from "../components/AuthForm.tsx";

const baseUrl = import.meta.env.BASE_URL;

if (Astro.locals.user) {
  return Astro.redirect("/");
}
---

<Layout
  title="Crear cuenta"
  description="Crea tu cuenta gratuita en Recetas de Casa para guardar favoritos, crear colecciones y desbloquear el menú semanal con IA."
  noindex={true}
>
  <main class="min-h-[calc(100vh-4rem)] bg-canvas py-16 sm:py-24">
    <div class="mx-auto max-w-md px-4 sm:px-6">
      <div class="mb-10 text-center">
        <p class="mb-2 text-xs font-semibold uppercase tracking-[0.2em] text-accent">Cuenta</p>
        <h1 class="font-display text-3xl font-semibold tracking-tight text-fg sm:text-4xl">
          Crear cuenta
        </h1>
        <p class="mt-3 text-sm text-fg-muted">
          Gratis y sin tarjeta. Guarda favoritos, organiza colecciones y prueba el menú semanal con IA.
        </p>
      </div>

      <AuthForm client:load mode="signup" baseUrl={baseUrl} />

      <p class="mt-8 text-center text-sm text-fg-muted">
        ¿Ya tienes cuenta?
        <a
          href={`${baseUrl}login`}
          class="font-medium text-accent transition-colors hover:text-accent-hover"
        >
          Iniciar sesión
        </a>
      </p>
    </div>
  </main>
</Layout>
